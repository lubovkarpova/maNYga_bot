# 🎅 Secret Santa Bot

Telegram-бот для организации Secret Santa (Тайного Санты) на вечеринке.

## Особенности

- ✅ Регистрация взрослых участников через Telegram
- ✅ Поддержка детей без Telegram (их регистрируют взрослые)
- ✅ Автоматическое создание назначений Secret Santa
- ✅ Отправка назначений всем участникам
- ✅ Защита от просмотра чужих назначений

## Требования

- Python 3.8+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Отредактируйте `.env` и добавьте:
```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
ADMIN_ID=ваш_telegram_id
```

Чтобы узнать свой Telegram ID, напишите боту [@userinfobot](https://t.me/userinfobot)

## Запуск

```bash
python secret_santa_bot.py
```

### ⚠️ Важно: Работа бота

**Локальный запуск (текущий вариант):**
- Бот работает только пока ваш компьютер включен и скрипт запущен
- Если вы закроете терминал или выключите компьютер, бот перестанет отвечать

**Для Secret Santa это нормально!** 
1. Запустите бота на время регистрации участников
2. После команды `/assign` все получат свои назначения в Telegram
3. Назначения уже отправлены, бота можно выключить

**Если нужен постоянный доступ:**
- Используйте облачный хостинг (VPS, Railway, Render, Heroku)
- См. раздел "Облачный хостинг" ниже

## Использование

### Для участников:

1. **Регистрация взрослого:**
   - Отправьте `/register` боту
   - Введите ваше имя

2. **Добавление ребенка:**
   - Отправьте `/add_child` боту
   - Введите имя ребенка
   - Ребенок будет участвовать, но его назначение получит взрослый, который его зарегистрировал

3. **Просмотр участников:**
   - Отправьте `/list` чтобы увидеть всех зарегистрированных

4. **Узнать свое назначение:**
   - После создания назначений отправьте `/my_assignment`

### Для администратора:

1. **Создать назначения:**
   - Когда все зарегистрируются, отправьте `/assign`
   - Бот автоматически создаст назначения и отправит их всем участникам

2. **Сбросить данные:**
   - Используйте `/reset` чтобы начать заново

## Команды

- `/start` - Начать работу с ботом
- `/register` - Зарегистрироваться как участник
- `/add_child` - Добавить ребенка (без Telegram)
- `/list` - Показать всех участников
- `/assign` - Создать назначения Secret Santa (только админ)
- `/my_assignment` - Узнать, кому ты даришь подарок
- `/reset` - Сбросить все данные (только админ)
- `/help` - Показать справку

## Как это работает

1. Все взрослые регистрируются через `/register`
2. Взрослые могут добавить детей через `/add_child` (дети без Telegram)
3. Администратор запускает `/assign` когда все готовы
4. Бот создает циклические назначения (каждый дарит следующему)
5. Каждый участник получает сообщение с именем того, кому нужно дарить подарок
6. Для детей назначение получает взрослый, который их зарегистрировал

## Пример использования

```
Участник 1: /register → "Иван"
Участник 2: /register → "Мария"
...
Участник 11: /register → "Петр"

Взрослый: /add_child → "Аня"
Взрослый: /add_child → "Максим"

Админ: /assign → Все получают назначения!
```

## Примечания

- Назначения создаются случайным образом, но гарантируется, что никто не дарит сам себе
- После создания назначений регистрация закрывается
- Только администратор может создавать назначения и сбрасывать данные
- Данные хранятся в памяти (при перезапуске бота данные теряются)

## Облачный хостинг (опционально)

Если нужен постоянный доступ к боту без включенного компьютера:

### Railway (рекомендуется, простой)
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект из GitHub репозитория
3. Добавьте переменные окружения: `TELEGRAM_BOT_TOKEN` и `ADMIN_ID`
4. Railway автоматически запустит бота

### Render
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый Web Service
3. Укажите команду запуска: `python secret_santa_bot.py`
4. Добавьте переменные окружения

### VPS (DigitalOcean, Hetzner)
1. Арендуйте VPS сервер
2. Установите Python и зависимости
3. Запустите бота через `screen` или `systemd` для работы в фоне

**Для одноразового Secret Santa облачный хостинг не обязателен!**

## Лицензия

MIT
